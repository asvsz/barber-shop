function ReserveCard({children}) {
  return (
    <div className="flex justify-between mx-auto items-center
    bg-white rounded overflow-hidden shadow-lg h-16 text-center">
        {children}
    </div>
  )
}

export default ReserveCard